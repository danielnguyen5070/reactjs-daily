// Build a Todo List application with the following requirements:

// - Add a new todo(input + button)
// - Mark a todo as completed / uncompleted
// - Delete a todo
// - Display total and completed count
// - Prevent unnecessary re - renders(optimize performance)
// - Store in localStorage

export default function App() {
	return (
		<div>
			Todo
		</div>
	)
}
